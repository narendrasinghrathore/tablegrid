import { TableColumnResizerDirective } from './table-column-resizer.directive';

describe('TableColumnResizerDirective', () => {
  it('should create an instance', () => {
    const directive = new TableColumnResizerDirective();
    expect(directive).toBeTruthy();
  });
});
